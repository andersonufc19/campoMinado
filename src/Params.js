import React from 'react'
import Icon from 'react-native-vector-icons'

import {Dimensions} from 'react-native'

//Esses valores podem ser alterados
const params = {
    blockSize: 30,
    borderSize: 5,
    fontSize: 15,
    headerRation: 0.15,     //Proporção do cabeçalho
    difficultLevel: 0.1,

}

export default params